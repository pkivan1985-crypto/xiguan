# 变更记录

## 3.0.0-rc.15（额外开支反思与记录安全候选）

- 新增事件驱动的“额外开支”习惯：有消费时才记录，没有消费无需打卡，也不计入漏打卡或每日计划。
- 支持同一天记录多笔开支，汇总显示今日与本月金额，并可填写购买内容、原因、银行余额、赚回天数、补偿代价和必要性等反思信息。
- 开支明细支持独立全屏填写、修改、删除和历史补录；归档后历史记录仍可查看，不丢失黄色消费标记。
- 进展月历用黄色圆点单独表示消费日期，与普通习惯的绿色完成标记并列显示；开支不计入绿色打卡次数、长期目标或总规划。
- 普通与加密备份兼容多笔开支聚合及旧版单笔数据，并校验汇总金额，降低恢复时明细与总额不一致的风险。
- 完善不同习惯类型的今日卡片、详情入口和图标状态，保持 Android 与 iOS 的触控和显示一致。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机功能验收，不是正式 `v3.0.0`。

## 3.0.0-rc.14（三页标题与月历布局统一候选）

- “今天”“进展”“习惯”三个主页面统一标题区高度、顶部位置、内容宽度和设置图标样式，页面切换时不再上下跳动。
- “进展”标题下新增本月成功打卡次数，让标题区信息密度与另外两个主页面保持一致。
- “习惯”页的设置与新建习惯操作改为同一行，移除原本造成标题区过高的上下堆叠。
- 成果月历增加日期行距和上下留白，打卡圆点与蓝色选中框拥有独立空间，避免窄屏或字体缩放时互相覆盖。
- 打卡、目标计算、行动记录、IndexedDB、备份 schema 与 Service Worker 安全更新策略保持不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机界面验收，不是正式 `v3.0.0`。

## 3.0.0-rc.13（每日记录界面精修候选）

- 今天页进一步压缩日期、周历、进度和操作区间距，底部主导航改为三个通用图标，并保留完整的读屏名称与 48px 触控区域。
- 跑步卡移除含义不清的减号和加号，只保留“详情”和一个圆形打卡按钮；未完成时为灰色，完成后同一按钮变绿并锁定。
- 跑步记录页重新整理实际公里数、计划、完成状态和上次记录，保留用时、平均配速、平均心率与备注等完整训练信息。
- 已完成记录继续遵守完成锁定规则，修改公里数或训练详情不会取消打卡状态。
- 轻食、喝水、阅读、睡眠、少刷手机等其它习惯的既有快捷记录与专属填写内容保持不变。
- IndexedDB、备份 schema 与 Service Worker 安全更新策略保持不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机界面和交互验收，不是正式 `v3.0.0`。

## 3.0.0-rc.12（打卡锁定与配速输入修复候选）

- 修复完成打卡后再次修改公里数、用时、配速、心率或备注会取消完成状态的问题；只有用户明确取消或删除记录才会解除完成。
- 今日记录、详细记录页和历史纠错统一使用分钟与秒数分段配速输入，格式固定为 `06 : 30 /km`，用户只需输入数字。
- 分钟输入两位后自动跳到秒数，清空秒数后按退格可返回分钟，减少手机键盘操作。
- 公里数、用时和心率继续使用普通数字输入与固定单位，保留今天计划和常用公里数快捷选择。
- 更新受影响的开发构建依赖锁定版本，关闭发布门禁发现的高危拒绝服务与解析漏洞；生产运行依赖不变。
- IndexedDB、备份 schema 与 Service Worker 安全更新策略保持不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机验收，不是正式 `v3.0.0`。

## 3.0.0-rc.11（跑步记录易用性修复候选）

- 修复手机端记录页面输入一个字符后输入框被重新挂载、键盘反复收起的问题；公里数、用时、配速、心率及其他习惯输入框均可连续填写。
- 新建当天跑步记录时默认带入最近一次实际公里数，用户可直接微调，减少重复输入。
- 保留“今天计划、3 km、5 km”等快捷数值；没有历史记录时使用今天计划，休息日保持空白，已有当天记录始终优先。
- 上一次的用时、平均配速和平均心率仅作为参考，必须点击“沿用上次”才会复制，备注不会自动沿用。
- 跑步公里数统一显示为 `1.00`、`3.60`、`3.334` 等有效精度，避免无意义的尾随零。
- IndexedDB、备份 schema 与 Service Worker 安全更新策略保持不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机输入体验验收，不是正式 `v3.0.0`。

## 3.0.0-rc.10（个性化习惯记录候选）

- 今天页按跑步、喝水、阅读、早睡、少刷手机和轻食等习惯类型提供对应的记录入口与填写内容。
- 新增轻食预设：不吃上火、辛辣、肥腻食物，不喝奶茶或饮料，并支持使用叶片图标连续添加多项自定义约束。
- 跑步记录继续支持实际公里数、时长、平均配速、平均心率和备注；完成状态与详细记录使用同一事实数据。
- 创建习惯扩充类型范围和预设，同时保持长期目标、可选阶段目标与每日安排的既有推导规则。
- 今天页卡片支持左滑露出右侧删除按钮；永久删除保留二次确认，并事务式清理习惯、目标、阶段和历史记录。
- 旧卡片与旧备份保持向后兼容，IndexedDB 与备份 schema 版本不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机验收，不是正式 `v3.0.0`。

## 3.0.0-rc.9（补打卡记录纠错候选）

- 进展月历中的已保存成果新增“修改记录”入口，补打卡保存后仍可直接纠错。
- 修改弹层预填原记录；跑步支持补充、修正或清空实际公里数、时长、平均配速、平均心率和备注。
- 修改使用原行动记录 ID 和同一事务更新，不创建重复记录，也不会重复累计目标进度。
- 确认修改后重新计算长期目标、阶段目标和成果月历；删除继续保留二次确认。
- 保留未来日期、记录日期不匹配和重复补打卡保护；IndexedDB 与备份 schema 版本不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机补打卡纠错验收，不是正式 `v3.0.0`。

## 3.0.0-rc.8（补打卡操作区对齐修复候选）

- 修复进展月历的“补打卡”和“查看详情”在手机端被固定高度卡片底边裁切的问题。
- 带日期详情的月历现在按实际内容自动增高，并保留完整底部留白。
- 两个操作项保持同一水平基线和 44px 触控高度；首页不带详情的纯月历继续使用原紧凑高度。
- 不改变补打卡业务规则、IndexedDB、备份 schema 或 Service Worker 更新策略。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机布局验收，不是正式 `v3.0.0`。

## 3.0.0-rc.7（历史日期补打卡候选）

- 进展月历为过去日期新增“补打卡”入口，直接复用当天习惯的打卡与实际进度记录体验。
- 仅允许补记当日原计划执行且尚未留下记录的习惯；未来日期、休息日、已有记录和习惯创建前日期不会出现补录入口。
- 跑步补录支持实际公里数、时长、平均配速、平均心率和备注，保存后同步更新历史、成果月历与目标进度。
- 历史补录使用独立的显式写入上下文，默认保存通道仍只允许今天，避免其他页面意外修改过去。
- 长期目标和阶段目标按补录日期匹配，并保持进行中阶段优先及阶段完成后自动接续规则。
- IndexedDB 与备份 schema 版本保持不变。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机补打卡验收，不是正式 `v3.0.0`。

## 3.0.0-rc.6（每日习惯与目标规划重构候选）

- 手机端主信息架构收敛为“今天、进展、习惯”三个入口，并统一暗色视觉层级、图标、卡片和底部导航。
- 今天页直接展示进行中的习惯；支持“打卡 → 完成”和“记录进度”，跑步可填写实际公里数、时长、平均配速、平均心率与备注。
- 实际进度少于计划时，差额顺延到下一次计划日；多出的进度计入阶段目标与长期目标总量。
- 休息日习惯仍会显示“今日休息”，但不计入今日任务总数且不可误记录。
- 进展页整合月历、日期成果、训练详情与总规划，并以阶段目标优先、长期目标在后的层级展示。
- 创建习惯由长期目标推导每日安排；长期目标与阶段目标支持天数和截止日期双向换算，分阶段推进可按需开启。
- 习惯页支持双列折叠浏览、展开详情、归档、恢复，以及带二次确认和关联数据事务清理的永久删除。
- IndexedDB 与备份 schema 版本不变；新增字段保持旧卡和旧备份向后兼容。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机功能和视觉验收，不是正式 `v3.0.0`。

## 3.0.0-rc.5（移动端布局与卡套折叠候选）

- 今日成果顶部改为明确的“保存后自动播放”说明，避免被误认为不可点击按钮。
- 首页“记录今日成果”与“创建循环卡”统一为同排快捷操作组。
- 卡套分类大项支持折叠，折叠后保留分类名称、卡片数量和可用状态。
- 分类展开后，循环卡默认一行两张；折叠卡保留名称、类型、进度百分比和进度条。
- 单张循环卡展开后独占整行并显示完整目标信息，其余卡继续保持双列。
- 不改变 IndexedDB schema、备份格式、成果保存、目标计算或 Service Worker 策略。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于 Android 与 iOS 同步实机视觉和交互验收，不是正式 `v3.0.0`。

## 3.0.0-rc.4（iOS 数据提示与首页创建入口修复候选）

- 修复已有循环卡后首页“创建循环卡”入口消失的问题。
- 保留“记录今日成果”主按钮，并增加常驻的轻量创建入口。
- iOS 安装提示明确说明 Safari 与主屏幕 PWA 使用隔离的本地数据。
- iOS Safari 与已安装 PWA 均可从设置页直接进入数据管理，完成备份和恢复。
- 不改变 IndexedDB schema、备份格式、业务记录或 Service Worker 更新策略。
- 候选站：<https://repeat-outcome.pages.dev>

此版本用于继续 iOS 真机与更新路径验收，不是正式 `v3.0.0`。

## 3.0.0-rc.3（安卓离线提示修复候选）

- Android 离线冷启动与原有本地记录读取已经通过真机验证。
- 离线状态提示增加由用户控制的“关闭”按钮，不再永久遮挡首页内容。
- 关闭仅作用于当前离线周期；恢复联网后再次离线会重新提示。
- 离线提示与版本更新提示使用独立关闭状态，不会互相隐藏。
- 候选站：<https://repeat-outcome.pages.dev>

此版本继续用于 Android 离线提示、真实文件恢复及 iOS 全路径验收，不是正式 `v3.0.0`。

## 3.0.0-rc.2（安卓真机修复候选）

- 修复当天纠错的二次确认层与编辑面板、底部导航重叠。
- 确认态现在只渲染一个不透明底部面板，并使用当前设计系统颜色变量。
- 候选站：<https://repeat-outcome.pages.dev>

此版本继续用于 Android/iOS 真机、离线、更新和真实文件恢复验收，不是正式 `v3.0.0`。

## 3.0.0-rc.1（公开候选）

- 完成卡套、目标、今日成果、首页月历、历史与当天纠错流程。
- 完成普通/加密备份、校验、预览、原子恢复与失败回滚。
- 完成由用户控制的 PWA 安装、离线和安全更新流程。
- 统一候选版本、项目名称、上游来源与 AGPL-3.0 说明。

- 公开源码：<https://github.com/pkivan1985-crypto/xiguan>
- 候选站：<https://repeat-outcome.pages.dev>

此版本仍等待 Android/iOS 真机、真实文件恢复和 RC 更新验收，不是正式 `v3.0.0`。
